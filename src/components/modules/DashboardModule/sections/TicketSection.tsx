type TicketSectionProps = {
   ticketPath?: string,
   page: number
}

const TicketSection: React.FC<TicketSectionProps> = ({ticketPath, page}) => {

    return <div></div>
}

export default TicketSection